// import fs from 'fs'

// import { RuleTester } from 'eslint'
// import BooleanCastStyleRule from './boolean-cast-style'

// const tester = new RuleTester({
//     parserOptions: { parser: '@babel/eslint-parser', ecmaVersion: 2021, sourceType: 'module' },
// })

// {

//     tester.run('no-lodash-named-imports', rule, {
//         valid: valideCodes,
//         invalid: invalideCodes.map(([code, output]) => ({
//             code,
//             errors: [{ message: 'Найдена ошибка' }],
//             output
//         }
//         ))
//     })
// }
it("fake", () => {
    expect(2 + 2).toBe(4);
});
